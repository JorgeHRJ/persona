{% extends 'cms/layout.html.twig' %}

{% page_setup {
    'title': "Nueva habilidad para #{group.name}",
    'header': "Nueva habilidad para #{group.name}",
    'subheader': "Rellena el formulario para crear una nueva habilidad para #{group.name}",
    'breadcrumbs': [
        {
            'name': 'Habilidades',
            'path': path('cms_skillgroup_index')
        },
        {
            'name': 'Nueva habilidad',
            'main': true
        }
    ]
} %}

{% block content %}
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    {{ include('cms/skill/components/form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
