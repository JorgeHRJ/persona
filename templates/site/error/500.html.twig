{% extends 'site/base.html.twig' %}

{% page_setup {
    'title': 'Error en el sistema',
    } %}

{% block body %}
    <main class="d-flex w-100 h-100">
        <div class="container d-flex flex-column">
            <div class="row vh-100">
                <div class="col-sm-10 col-md-8 col-lg-6 mx-auto d-table h-100">
                    <div class="d-table-cell align-middle">
                        <div class="text-center">
                            <h1 class="display-1 fw-bold">Ups...</h1>
                            <p class="h1">Error en el sistema</p>
                            <p class="h2 fw-normal mt-3 mb-4">
                                Parece que hemos tenido algún error en el sistema. Si este error continúa, por favor,
                                ponte en contacto con el administrador.
                            </p>
                            <a href="{{ path('site_about_index') }}" class="btn btn-primary btn-lg">Volver</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
{% endblock %}
